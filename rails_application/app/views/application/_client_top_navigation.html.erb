<nav class="bg-gray-800">
  <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex items-center justify-between h-16">
      <div class="flex items-center">
        <div class="flex-shrink-0">
          <h1 class="text-gray-600 uppercase tracking-tight font-bold">
            ecommerce
          </h1>
        </div>
        <div class="hidden md:block">
          <div class="ml-10 flex items-baseline space-x-4">
            <%= navigation_link "Client View", clients_path %>
            <%= navigation_link "Products", client_products_path %>
            <%= navigation_link "Inbox", client_inbox_path %>
          </div>
        </div>
      </div>
      <div class="flex items-center">
        <%= form_with url: switch_store_path, method: :post, class: "inline-flex items-center" do |f| %>
          <%= f.select :store_id,
                       options_from_collection_for_select(available_stores, :id, :name, current_store_id),
                       {},
                       class: "bg-gray-700 text-white border-gray-600 rounded-md px-3 py-1 text-sm focus:ring-blue-500 focus:border-blue-500",
                       onchange: "this.form.submit()" %>
        <% end %>
      </div>
    </div>
  </div>
</nav>
